//Legibilidade

let x = 1.89; // variável com nome pouco significativo, não sabemos o que é esse valor

let altura = 1.89; // variável com nome significativo, sabemos que é a altura de algo ou alguém

altura = 7;
largura = 10;
area = 70;

//letras maiúculas e minúsculas fazem diferença

preco = 10;
Preco = 20;

console.log(preco); // Exibe o valor da variável "preco" no console do navegador
console.log(Preco); // Exibe o valor da variável "Preco" no console do navegador
console.log(precO); // Exibe o valor da variável "precO" no console do navegador, mas como essa variável não foi definida, resultará em um erro

idadeUsuario = 20;
nomeCompleto = "João Guilherme Costa Soares";
